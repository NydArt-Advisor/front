export const metadata = {
    title: 'Settings - NydArt Advisor',
    description: 'Manage your account preferences and security settings',
};

export default function SettingsLayout({ children }) {
    return children;
} 